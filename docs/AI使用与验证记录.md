# AI 使用与验证记录

本文件记录「投资命题多证据验证器」交付过程中 AI（Claude Code）的实际使用方式，
重点是**它出过哪些错、怎么被发现、怎么被修**，以及在哪些判断上不能由它替代人。

写作约定：本文件里出现的每一处代码位置都在写作时打开文件核对过；
每一个数字都来自实跑输出，不引用未复核的资料。无法核实的说法不写。

---

## 0. 一个必须先说明的前提

本仓库的 git 历史只有**一个提交**（`0efc38f`，2026-09-23 17:37），
开发过程没有保留中间提交。因此「AI 曾经写错过什么」这件事**无法用 `git diff` 复现**。

本文中所有「曾经的错误」都有第二类证据支撑，两者之一：

1. **修复时写在代码里的注释**——注释直接记下了错的现象与根因，例如
   `app/engine/parse.py:61` 的「已实测踩过」、`app/engine/pipeline.py:532` 的
   「早先这里是拿年报重算一遍的」；
2. **提交信息里逐条列出的 8 处缺陷**（`git log -1 0efc38f` 可见）。

凡是没有这两类证据的，本文不写成「AI 犯过的错」。

---

## 1. AI 承担了什么

- **写实现代码**：后端全链路（`app/` 下 23 个文件、约 5200 行），
  含命题解析、模板实例化、单次取数编排、6+7+5 个执行器、冲突检测、结论聚合、反转条件表。
- **把产品约束翻译成类型约束**：`app/schemas.py` 里的 `model_validator` /
  `field_validator`（见第 3 节，这类校验最终反过来抓出了实现层的错）。
- **查接口与写取数层**：按扶摇接口契约实现 `FuyaoClient`、错误码到
  `UnverifiableCategory` 的映射表（`app/providers/base.py:42` 起）、
  冻结快照捕获脚本 `scripts/capture.py`。
- **写测试套件**：`backend/tests/`（本次交付的一部分，168 个测试）。
- **做机械性核查**：跨报告期配对、同口径同比、数字与证据卡的一致性对账。

## 2. AI 没有承担什么（人工决定的部分）

- **命题类型学与子问题清单**：三类命题（背离 / 归因 / 传导）各自应该问哪几个子问题，
  是人工定的，写死在 `app/templates/gold.py`（18 条子问题）。AI 只做「实例化」。
- **全部判定阈值**：`2pp`、`0.8 倍`、`5pp`、`3pp`、`±1pp` 这些数字由人工定稿并写进模板，
  AI 不得在运行时另取阈值（见第 5 节）。
- **「什么算基本面恶化」这个口径选择**：见第 5 节第 2 条。
- **产品措辞边界**：结论只能出现「现有证据支持 / 不支持 / 证据不足」，不许出现买卖建议。
- **数据源能力边界的判断**：哪些数据扶摇接口确实没有，由人工核查接口清单后确认，
  AI 只负责把结论写进 `skipped_layers` 与无法验证的三问里。

---

## 3. AI 出过的错：现象、发现方式、修复位置

以下 10 条按「同一类错的家族」排列。每条都给出代码位置，可逐条打开核对。
其中 3.1–3.8 对应提交信息里列出的 8 处（3.5 与 3.6 在提交信息里合为一条），
3.9、3.10 是提交信息里没列、只在代码注释里留下记录的。

### 3.1 判了「无法验证」却不写清为什么

- **现象**：早期执行器在某些分支里直接把证据判为 `UNVERIFIABLE`，
  但 `unverifiable` 详情留空。「失败必须透明」这条硬约束在运行期被绕过了——
  证据卡上只有一句「无法验证」，用户不知道是缺数据、还是没这项能力、还是还没披露。
- **发现方式**：**类型层自己抓出来的**。`app/schemas.py:256` 的
  `Evidence._unverifiable_needs_detail` 规定「判为无法验证必须带三问详情；
  非无法验证不得带详情」，于是这些调用点在构造 `Evidence` 时当场抛 `ValidationError`，
  链路直接报错，而不是产出一张信息残缺的证据卡。
- **修复**：不是简单补一段文字，而是先补了一个原因分类
  `UnverifiableCategory.INCONCLUSIVE_RANGE`（`app/schemas.py:58`）——
  「数据取到了，但落在判定规则的中性区间」。因为原先的三问模板回答不了这一类：
  它需要回答的不是「缺什么数据」，而是「这个指标得走出中性区间才有方向性」。
  随后在 5 个执行器里补了 6 处详情（`app/engine/executors.py` 的 453 / 551 /
  631 / 639 / 691 / 842 行；其中 631 与 639 属同一个执行器 `ex_att_04` 的两个分支），
  统一走 `_inconclusive()`（`app/engine/executors.py:139`）。
- **实测对照**：贵州茅台的净利润现金含量 0.75 倍就落在这个区间——
  数据完整、口径一致，但判据「≥0.8 支持 / 0.5–0.8 中性 / <0.5 反对」在该值上不做方向性判断。
  跑出来的证据卡如实写明 `inconclusive_range`，而不是硬给一个支持或反对。

### 3.2 同比取数取到了陈旧年报，漏掉最新季报

- **现象**：同比证据卡比实际数据晚三个季度。原因是只从年报序列里取最新一期，
  而那已经是半年前（甚至更早）的经营状况。对「基本面有没有恶化」这个问题，
  半年报数据可能已经完全过时。
- **发现方式**：人工对照该标的最新披露进度逐条核对证据卡的报告期时发现
  （证据卡写的是上年年报，而公司已经出了两个季报）。
- **修复**：新增 `_latest_comparable()`（`app/engine/executors.py:232`），
  在「季报最新一期 vs 上年同期」与「最新年报 vs 上一年报」之间选**更新的那一对**——
  两者都满足「同 `fiscal_period`」这个口径一致要求，所以取新的不会牺牲可比性；
  季报缺失或上年同期可比不了时才退回年报。
- **实测对照**：修复后 4 个标的的同比证据报告期都是 `2026Q2 vs 2025Q2`，
  而不是 `2025FY vs 2024FY`。测试里对此有硬断言
  （`tests/test_offline_pipeline.py::test_revenue_and_profit_evidence_share_the_same_period`）。

### 3.3 现金流表与利润表各取各的最新一期，分子分母不同期

- **现象**：「净利润现金含量 = 经营现金流净额 ÷ 归母净利润」这个比值，
  分子来自现金流量表、分母来自利润表。两者各取各的最新一期时，
  一旦两张表的披露进度不一致（例如现金流到 2026Q2、利润到 2025FY），
  算出来的比值**不属于任何一个时点**，是一个没有意义的数字。
- **发现方式**：人工逐条核对数据来源时发现两张表的报告期不同。
  代码注释把这一点写在了修复处（`app/engine/executors.py` 中 `ex_att_05` 上方）：
  「此前两者各取各的最新一期，在跨期披露不一致时会把 2026Q2 的现金流除以 2025FY 的利润」。
- **修复**：新增 `_match_period()`（`app/engine/executors.py:209`），
  强制在另一张报表里找**同一 `fiscal_year` + `fiscal_period`** 的那一期；
  找不到就返回 `None`，由调用方如实报缺失，**不做跨期拼凑**。

### 3.4 反转条件表另走一条计算路径，与证据卡数字打架

- **现象**：同一个指标在同一页上出现两个数字／两个报告期。具体表现是证据卡用的是
  最新季报（2026Q2），反转条件表却是拿年报重算了一遍（2025FY），
  「当前值」一栏因此与它上面那张证据卡不一致。
- **发现方式**：人工把反转条件表的每一个「当前值」拿去和证据卡逐条对账时发现。
- **修复**：反转条件表的当前值**一律从证据对象里读**，不再另算一遍。
  这一点被写成了 `app/engine/pipeline.py:531` 处的设计说明：
  「当前值一律从证据对象里读，不另走一条计算路径……从证据读值让『一致性』成为结构保证，
  而不是靠两处代码记得同步改」。
  取值统一走 `pick()`（`app/engine/pipeline.py:540`）。
- **实测对照**：4 个预设命题的反转条件表当前值与证据卡逐条对得上（贵州茅台
  `+1.47%` / `-1.95%` / `0.75 倍`，中国平安 `+15.01%` / `+36.06%` / `4.89 倍`）。
  **注意**：这个修复只覆盖了背离型，归因型仍有残留缺陷，见第 6 节 D-1、D-2。

### 3.5 中文环境下 `\b(\d{6})\b` 匹配不到紧跟汉字的股票代码

- **现象**：命题「我觉得600519.SH估值偏低了」里抽不出代码 600519。
- **根因**：Python 的 `\w` 在 `str` 模式下**包含中日韩汉字**，「得」和「6」都是 `\w`，
  两者之间不构成词边界，`\b` 因此匹配失败。
- **发现方式**：用这句中文命题实跑，发现 `ticker` 为空。
- **修复**：改用只依赖数字边界的写法 `(?<!\d)(\d{6})(?!\d)`（`app/engine/parse.py:63`），
  并在上一行（`parse.py:61`）写明「不能写 `\b(\d{6})\b`……已实测踩过」。
- **回归测试**：`tests/test_parse.py::test_chinese_word_boundary_regression`
  同时断言「老写法在该句上返回 None」和「新写法能匹配到 600519」——
  如果哪天有人把它改回 `\b`，这条测试会红。

### 3.6 标的名称抽取把「我认为贵州茅」当成了公司名

- **现象**：命题「我认为贵州茅台估值已经回落但基本面并没有恶化」，
  抽出的标的名是「我认为贵州茅」这种不存在的东西。
- **根因**：中文公司名没有形态学边界，早期实现按固定长度从句首截取。
- **修复**：两层。
  一是先剥掉口语前缀（`_SUBJECT_PREFIXES`，`app/engine/parse.py:107`，
  含「我认为 / 我觉得 / 帮我看看」等）；
  二是兜底路径改为先定位**谓述起点**（`_SUBJECT_NOISE`，`parse.py:112`），
  只取它前面那一小段，而不是扫全句——`app/engine/parse.py:148` 的注释记着原因：
  「只扫全句会把『已经回落但基』这类描述片段当名字，那是错的（已实际发生过）」。
- **回归测试**：`tests/test_parse.py::test_detect_subject_strips_colloquial_prefix`
  断言结果等于「贵州茅台」，且明确断言不等于「我认为贵州茅」。

### 3.7 传导型关键词表不全，把传导型命题误判成归因型

- **现象**：「我认为碳酸锂价格上涨会挤压宁德时代的利润空间」被判为**归因型**。
  类型判错，后面整套子问题都错。
- **根因**：传导型词表只收了「产业链 / 传导 / 上游 / 下游 / 涨价」这类书面词，
  漏掉了实际口语里更常见的「价格上涨 / 挤压 / 利润空间」。
- **修复**：补齐词表并在 `app/engine/parse.py:53` 注明
  「以下为实测补充：原词表漏掉了这批表述」。
- **回归测试**：三条——
  `test_detect_type_transmission_keywords_cover_price_squeeze_wording` 直接对词表下断言
  （这三个词必须在传导型词表里、且必须**不在**归因型词表里），
  `test_transmission_is_not_misjudged_as_attribution` 做端到端确认，
  外加 4 个预设命题的整链路断言。

### 3.8 结论措辞写死，与证据卡自相矛盾

- **现象**：归因型的结论陈述里写死了「收入端与毛利率端未能提供支持」。
  当收入端实际是**支持**态时，结论陈述就和同一页上的证据卡打架。
- **发现方式**：拿宁德时代的归因型命题实跑时暴露。
- **修复**：结论陈述按**实际三态**生成，而不是套固定句式。
  `app/engine/pipeline.py:347` 的注释记着这件事
  （「当收入端其实是支持态时，结论陈述会与证据卡自相矛盾（宁德时代实测踩到）」）。
- **实测对照**：宁德时代归因型跑出来是「虽然非经常性损益对利润的贡献不显著……
  但 **毛利率端** 已转向恶化」，只点了确实反对的那一项；营收同比 `+54.80%` 是支持态，
  没有被写进反对清单。

### 3.9 执行器抛出的异常没有透出到 errors，失败被静默吞掉

- **现象**：`errors` 只收集 collect（取数）阶段的失败。执行器在 collect 之后运行，
  它自己抛出的异常被 `try/except` 吞掉后只留了一行日志，**没有进入返回值的 `errors`**。
  用户看到的是一次「零错误」但结论可疑的运行——这比报错更危险。
- **发现方式**：人工读返回结构时发现「errors 为空」与「某条子问题没有对应证据」可以同时成立。
- **修复**：执行器异常同时写入 `ctx.run_errors` 并产出一条**留痕证据**
  （`_crash_evidence`，`app/engine/run.py:183`），
  最终在 `run.py:303` 并入 `errors`。两处注释记着这件事：
  `run.py:177`「执行器自身出错也必须留痕」、
  `run.py:302`「只收集 collect 阶段的错误会让执行期的失败静默消失」。
- **回归测试**：`tests/test_offline_pipeline.py` 中「零错误」与「每条证据都可溯源」
  是两条独立断言——如果异常又被吞掉，第二条会因为证据缺失而红。

### 3.10 离线模式的取数路径与实时不一致（代码注释里有、提交信息里没列）

- **现象**：离线模式静默缺一路数据——`annual` 请求去找一个不存在的
  `cash_flow_statements_annual.json`，于是保存的结论与实时模式不同。
- **修复**：`FixtureProvider.cash_flow_statements` 的 fixture 键名与
  `scripts/capture.py` 写入的键名严格对齐，并在 `app/providers/fixture.py` 的对应位置
  注明「此前 annual 请求去找不存在的 `cash_flow_statements_annual`，
  导致离线跑出来的结论与实时不一致」。
- **回归测试**：`tests/test_offline_pipeline.py` 中对每一路取数都有断言，
  且断言「缺快照时必须回 `Err`，不许回空列表」；
  另有 `test_captured_fixtures_are_available` 作为对照组，防止上述断言只是因为路径写错才通过。

---

## 4. 测试与验证手段（本次交付新增）

原先没有任何自动化测试。本次补了 `backend/tests/`，共 **168 个测试**
（162 passed + 6 xfailed），全部**离线**运行：

```bash
cd backend && python -m pytest tests -q
# 162 passed, 6 xfailed in ~1.1s
```

- 四个测试文件：`conftest.py`、`test_contracts.py`、`test_parse.py`、`test_offline_pipeline.py`。
- **离线性由代码保证**，不靠自觉：`conftest.py` 里的 autouse 装置会
  清掉 `ANTHROPIC_API_KEY` / `HITHINK_FINANCE_API_KEY`，
  并**把 `socket.connect` 直接替换成抛异常**——任何测试只要试图联网就当场失败。
- 覆盖层次：契约层（schema 构造失败）/ 解析层（含 3.5、3.6、3.7 三条回归）/
  离线全链路（4 个预设命题）/ 一致性（证据卡与反转条件表对账）/ 失败路径 / 合规边界。
- 6 条 xfailed 是**已知且尚未修复**的缺陷，每条都带 `strict=True`：
  一旦有人修好，测试会翻红提醒删掉标记。清单见第 6 节。

结论层面的实测结果（离线冻结快照）：

| 命题 | 类型 | 结论 | 关键证据 |
|---|---|---|---|
| 贵州茅台估值回落但基本面未恶化 | 背离 | **不支持** | 营收同比 +1.47%，归母净利同比 −1.95%（增收不增利） |
| 中国平安估值回落但基本面未恶化 | 背离 | **支持** | 营收 +15.01%，归母净利 +36.06%，PE 处 21.3 分位 |
| 宁德时代盈利改善来自主营业务 | 归因 | **不支持** | 核心判据 1.20pp 通过，但毛利率同比 −1.09pp |
| 碳酸锂涨价挤压宁德时代利润空间 | 传导 | **证据不足** | 3 条子问题因数据源能力边界无法验证 |

---

## 5. 哪些结论是 AI 不能替代人工判断的

以下每一条都是**人定的**，AI 的职责只是把人的决定落到代码与措辞里。

### 5.1 阈值为什么取这些数

- **非经常性损益阈值 2pp**：判据是「加权 ROE − 扣非加权 ROE > 2pp 即认为利润里非主营成分显著」。
  这个 2pp 人工定稿在模板里（`app/templates/gold.py` 的 SQ-03），
  反转条件表的阈值依据如实指向它、并声明「该阈值在模板中事先写定，不随个案调整」
  （`app/engine/pipeline.py:623` 附近）。AI 没有参与选这个数。
- **净利润现金含量 0.8 倍**：三态判据是「≥0.8 支持 / 0.5–0.8 中性 / <0.5 反对」。
  因此能翻转结论的阈值取的是 **0.8（跌出支持区间）而不是 0.5**——
  否则会出现「当前 0.75 倍显示成还没触发、但结论其实早已不在支持下」的自相矛盾。
  这处推理写在 `app/engine/pipeline.py:585` 附近的注释里。
- **周期性代理阈值 5pp、成本率变动阈值 3pp、毛利率同比 ±1pp**：同样人工定稿在
  `app/templates/gold.py`，并要求每条反转条件的「阈值依据」写明出处，
  禁止写成「显著变化时需重估」这类无法执行的空话（测试里有对应断言）。

### 5.2 「基本面」以归母净利润还是营收为主判据

这是本产品最核心的一次口径选择，**由人决定**：

命题原话是「估值回落**但基本面并没有恶化**」。营收增长而归母净利润下滑时，
「基本面有没有恶化」的答案取决于把「基本面」定义成经营规模还是股东获利能力。
产品选择的是**后者**——以归母净利润同比为主判据，收入端作为背景证据保留，
两者并列展示、不折叠成单一数字。理由与残余不确定性写在
`app/engine/pipeline.py:164` 附近的冲突裁决正文里；
判定逻辑对应 `_decide()` 中背离型的分支（基本面侧任一项被证伪即判不支持）。

AI 能做的是把这个选择一致地落到代码、措辞与反转条件表里；
**选哪一边是产品立场，不是技术判断**。

### 5.3 重建口径的容差与「宁可说不可验证」

- **自校准容差 35%**（`app/analysis/valuation_series.py:233`）是人工定的：
  重建 PE 与官方 `pe_ttm` 的相对偏差超过它，就把该条证据降级为中等置信度、
  只读趋势不读绝对水平，并触发一条口径冲突。这个数决定了产品有多「敢用」自己的重建序列。
- **「数据源没有就说没有」这条立场**是人工定的。传导型命题的多数子问题因此被判为
  结构性不可得，而不是编一段产业分析带过。这是一次**主动放弃覆盖率**的产品决策。

---

## 6. 已知缺陷（尚未修复）

### 6.1 本次写测试时新发现的 4 处

以下 4 处是本次补测试时的**实测发现**，不是历史遗留推测。均已用严格 xfail 钉住，
修好后测试会翻红提醒。

**D-1 反转条件表按子问题 ID 取值，未按命题类型区分（影响归因型）**
`app/engine/pipeline.py:554` 的 `pick("SQ-03", "SQ-01")` 与 `:571` 的 `pick("SQ-04")`
在背离型下含义正确（SQ-03 = 营收同比，SQ-04 = 归母净利同比），
但在归因型下 SQ-03 是**非经常性损益**、SQ-04 是**期间费用率**。
实测后果：宁德时代归因型的反转条件表里，
「营业收入同比增速」一栏填的是 `+1.20%（2026-2）`（ROE 差额），
「归母净利润同比增速」一栏填的是 `−2.35%（2026Q2 vs 2025Q2）`（费用率变动），
而真正的营收同比 `+54.80%` 在表里根本不出现。标题与数字对不上，阈值「由正转负」也不适用。

**D-2 现金含量行末位数字不一致**
`pick()` 取的是 `round(v, 3)` 后的证据值，再用 `f"{cash:.2f}"` 格式化；
证据卡则直接对未取整的 `v` 做 `f"{v:.2f}"`。
两条格式化路径在临界值上会差最后一位：宁德时代归因型的现金含量，
证据卡显示 `1.85 倍`，反转条件表显示 `1.84 倍（2025FY）`。

**D-3 冲突检测只按子问题 ID 绑定证据，不校验命题类型**
`app/engine/pipeline.py:113` 的冲突 3 假定 `SQ-06` 是「毛利率多期离散度」。
归因型里 SQ-06 是「少数股东损益占净利润」，于是周期性冲突的正文写成
「估值分位证据显示 PE 处于历史 `+54.80%`……周期性检测显示毛利率离散度 `5.97pp`」——
引用的两条证据（营收同比、少数股东占比）都不是它说的东西。
`app/engine/pipeline.py:86` 的冲突 2 同类：传导型命题里 SQ-01 是「主营构成不可得」，
仍因 SQ-01 存在而触发，并把这条证据当作重建 PE 的证据引用。

**D-4 `UnverifiableDetail` 的三问允许填空串**
`app/schemas.py:116` 的三个字段是裸 `str`，没有像 `Provenance._not_blank`
（`app/schemas.py:110`）那样拒绝空串。于是「需要什么 / 何时何地可得 / 失败证据」
可以填三个空白串照样构造成功，与 schema 声明「缺一条就构造不出来」不符。

### 6.2 交付状态核对（写作时点的 `git status`）

- `backend/fixtures/`（4 个标的的真实调用快照）与 `frontend/` **未被 git 跟踪**。
  这意味着**新克隆的仓库跑不了离线测试，也跑不了离线演示**——
  `FixtureProvider` 会把每一路取数都如实报成「快照缺失」。
  离线测试是本项目「不联网可复现」主张的载体，快照必须随仓库一起交付。
- `app/engine/pipeline.py`、`app/engine/run.py`、`app/providers/fixture.py`、
  `app/schemas.py` 有**未提交的改动**（本次记录写作时观察到的工作区状态）。
  本文引用的行号以该状态为准。

---

## 7. 人是如何验证 AI 的产出的

1. **跑真实接口，不看故事**。4 个标的的结论是在接上真实扶摇接口后实跑得到的，
   不是离线构造的。离线快照是**实跑之后**才捕获的
   （`scripts/capture.py`，捕获时间戳 `2026-09-23T09:37Z`，
   每份快照都带原始 `_request_id` 与 `_endpoint`、`_params`）。
   第 4 节表格里的每一行都对应一次实跑。
2. **对照官方 `pe_ttm` 做自校准**。重建 PE 序列与扶摇估值快照的官方 `pe_ttm` 逐个对拍，
   容差 35%（`app/analysis/valuation_series.py:233`）。实测偏差：

   | 标的 | 重建 PE | 官方 pe_ttm | 相对偏差 | 结果 |
   |---|---|---|---|---|
   | 600519.SH | 17.41 | 19.21 | 9.35% | 容差内 |
   | 601318.SH | 7.84 | 6.12 | 28.04% | 容差内（但已接近） |
   | 002594.SZ | 18.60 | 26.59 | 30.04% | 容差内 |
   | 300750.SZ | 23.47 | 16.39 | 43.25% | **超出容差** |

   宁德时代超出容差时，产品按预定规则降级处理：该条证据降为**中等置信度**，
   并触发一条口径冲突、在 UI 上把重建值与官方值并列展示（不做合并或平均）。
   这正是自校准机制存在的意义——它抓出了「我们的重建方法在这个标的上系统性偏高」。
3. **逐条核对数据来源**。每张证据卡都必须带上
   来源 / 接口 / 报告期 / 口径 / 单位五项溯源（`Provenance` 在类型层强制非空）。
   第 3.2、3.3 两处错误就是这样发现的：不是看结论对不对，
   而是看**证据卡上的报告期和分母分子的报告期是否自洽**。
4. **把「一致性」变成可执行的断言**。本次补的测试里有两条不做业务判断、
   只做对账的测试：反转条件表里每个数字必须能在证据卡的展示值里找到；
   标注的报告期必须能在某条证据的溯源里找到。这两条不依赖任何人的记忆，
   是防止第 3.4 类错误复发的主要手段。
5. **拿四个标的做交叉验证**。同一个模板套到 4 个不同标的（含一个超容差标的），
   互相对照才能区分「模板写错了」和「这个标的确实如此」。

---

## 8. 一句话总结

AI 在本项目里承担的是**把人的产品判断落成可执行、可校验的代码**；
它出的错集中在两类——**跨期的机械性错误**（取数、配对、格式化）
和**中文自然语言处理的经验性错误**（词边界、公司名边界、关键词覆盖）。
这两类恰好都能用「把约束写进类型层 + 用确定性测试对账」压住，
而不能压住的那部分（阈值、口径、要不要诚实地说不可验证）自始至终由人决定。

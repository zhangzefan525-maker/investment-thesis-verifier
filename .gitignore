# 凭据 —— 任何情况下都不进仓库
.env
*.env
credentials.env
!*.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Node
node_modules/
.vite/

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 构建产物与压缩包
*.zip
*.7z
build/
/dist/

# frontend/dist 必须进仓库：评审者不装 node_modules 也应能直接跑起前端。
# 注意不能写 `frontend/dist/`——目录一旦被排除，后面的 ! 取反对它无效。
# 这里用根锚定的 `/dist/`，只排除仓库根目录下的构建产物。

# 演示视频不进版本控制，理由是算出来的：
# 它现在是 frontend/scripts/video/ 那条流水线的**产物**，产物不需要版本控制，
# 产出它的那条流水线才需要。而它一旦进 git，交付用的压缩包里就会同时装着
# 「这个文件」和「git 里的那一份」，一份 9 MiB 的视频要占 18 MiB 的额度；
# 而且历史里那份永远删不掉（已有一版 8.95 MiB 的旧视频留在 commit a3acee6 上，
# 正是本仓库要能复核的那批提交之一，不能动）。视频本身随压缩包一起交付。
演示视频.mp4
